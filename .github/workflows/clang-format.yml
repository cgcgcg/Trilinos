name: Check clang-format

on:
  pull_request:
    branches: ["develop"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    # Check out the PR
    - name: Checkout
      uses: actions/checkout@v4

    # Run clang-format over it, modify in-place
    - name: clang-format
      uses: DoozyX/clang-format-lint-action@v0.16.2
      with:
        source: './packages/muelu'
        exclude: './packages/muelu/src/MueCentral/MueLu_MasterList.cpp'
        extensions: 'cpp,hpp'
        clangFormatVersion: 14
        style: file
        inplace: true

    # Re-run clang-format over it, modify in-place
    # Clang-format is not idempotent?!?
    - name: clang-format
      uses: DoozyX/clang-format-lint-action@v0.16.2
      with:
        source: './packages/muelu'
        exclude: './packages/muelu/src/MueCentral/MueLu_MasterList.cpp'
        extensions: 'cpp,hpp'
        clangFormatVersion: 14
        style: file
        inplace: true

    # Open up a new PR from "push-to-fork" to the base of the PR
    - name: Create Pull Request
      id: cpr
      uses: peter-evans/create-pull-request@v5
      with:
        # push-to-fork: cgcgcg/Trilinos
        # token: ${{ secrets.MACHINE_USER_PAT }}
        delete-branch: true
        base: ${{ github.head_ref }}
        commit-message: clang-format
        branch: clang-format-fix
        branch-suffix: timestamp
        title: Automated clang-format fixup of PR ${{ github.event.pull_request.number }}
        body: Automated clang-format fixup of PR #${{ github.event.pull_request.number }}

    # Comment on the PR with details of how to pull in the changes
    - name: Comment
      if: ${{ steps.cpr.outputs.pull-request-number }}
      uses: peter-evans/create-or-update-comment@v3
      with:
        issue-number: ${{ github.event.pull_request.number }}
        body: |
          clang-format diff'd!

          I opened a PR against your branch that fixes the formatting. Have a look at #${{ steps.cpr.outputs.pull-request-number }} and merge it if the changes look good.
